import React, { Component } from "react";

class Standings extends Component {
  render() {
    return <div className={`${this.props.className}`}>Dummy content</div>;
  }
}

export default Standings;
